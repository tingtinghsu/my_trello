<div class="flex my-2" id="show-list" data-lists="<%= @lists.to_json(include: :cards) %>">
  <List v-for="list in lists" :list="list" :key="list.id"></List>
  <!-- 把component的資料餵給html -->
</div>

<br>

<%= link_to '新增list', new_list_path, class: 'mx-2' %>
